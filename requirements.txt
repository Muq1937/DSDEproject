# Core data processing
pandas>=2.0.0
numpy>=1.24.0
dask[complete]>=2023.5.0

# Visualization
plotly>=5.14.0
dash>=2.14.0
dash-bootstrap-components>=1.4.0
folium>=0.14.0
branca>=0.6.0

# Network analysis
networkx>=3.0
python-louvain>=0.16

# Geospatial (optional - only needed for advanced features)
# Uncomment if you need geopandas (harder to install on Windows)
# geopandas>=0.12.0
# shapely>=2.0.0

# Data serialization (for caching)
pyarrow>=10.0.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.65.0
